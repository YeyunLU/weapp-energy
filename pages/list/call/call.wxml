<view class="btn">
 
 <view wx:for="{{calls}}">
    <button size="default" type="default" bindtap="calling">{{item.name}}</button>
</view>

 <button size="default" type="default" bindtap="adding">+添加亲友电话</button>

</view>